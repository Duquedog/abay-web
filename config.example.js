// config.example.js — copy this file to config.js and fill in your real values
// config.js is gitignored and must never be committed
window.CRM_CONFIG = {
  apiUrl: 'https://YOUR-BACKEND.onrender.com',
  apiKey: 'YOUR_PUBLIC_WEB_KEY'
};
